# 💳 Управление банковскими картами

Бэкенд-приложение, созданное на **Java (Spring Boot)**, для управления банковскими картами.
Оно позволяет администраторам управлять пользователями и картами, а пользователям — выполнять безопасные операции со своими картами, такие как просмотр баланса, перевод средств и запрос на блокировку карт.
Безопасность обеспечивается **аутентификацией JWT, доступом на основе ролей** и **шифрованием данных**.

____

## Features

### Аутентификация и авторизация

- Spring Security + JWT + DTO
- Роли: ADMIN и USER

### Админ

- Создать, заблокровать, активировать, удалить карты
- CRUD операции для пользователей
- Смотреть все карты

### Пользователь

- Просмотр собственных карт (поиск + постраничная навигация)
- Запрос блокировки карты
- Перевод средств между собственными картами
- Проверка баланса

### API

- Операции CRUD над картами
- Переводы между собственными картами
- Фильтрация и постраничная навигация
- Валидация и обработка ошибок

### Безопасность

- Шифрование данных
- Контроль доступа на основе ролей
- Маска номера карты
____

### Технологический стек

**Java 21, Spring Boot, Spring Security, JWT, MySQL, Liquibase, jUnit tests, Docker**

### API Документация

[Swagger API Docs](http://localhost:8080/swagger-ui.html)

____

## Как запустить

1. **Настройка переменных окружения**
   Создайте файл .env или установите переменные непосредственно в своей среде исполнения:
   PG_HOST=db
   PG_PORT=5432
   PG_DB=bank_rest
   PG_USERNAME=postgres
   PG_PASSWORD=1234   

    ```
    JWT_SECRET=сгенерированный_jwt_secret
    PG_HOST=localhost
    PG_PORT=5432
    PG_DB=bank_rest
    PG_USERNAME=postgres
    PG_PASSWORD=ваш_пароль

2. **Сборка и запуск с использованием Docker Compose**

    ```
    docker-compose up --build
    ```

### Структура проекта

```
src/
├── main/java/com/example/bankcards
│    ├── config        # Security & application config
│    ├── controller    # REST controllers
│    ├── dto           # Data Transfer Objects
│    ├── entity        # JPA entities
│    ├── exception     # Custom exception handlers
│    ├── repository    # Spring Data JPA repositories
│    ├── security      # JWT authentication & authorization
│    ├── service       # Business logic
│    └── util          # Utility classes
└── test/java          # Unit tests
```